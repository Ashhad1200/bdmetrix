export const metadata = {
  title: 'Contact Us | BD Matrix',
  description: 'Get a free 30-minute consultation. Tell us about your project and we will get back to you within 24 hours.',
};

export default function ContactLayout({ children }) {
  return children;
}
